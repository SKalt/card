{
  "name": "happy-holidays.now.sh",
  "version": "0.0.1",
  "description": "a holiday card",
  "main": "src/index.html",
  "repository": "https://github.com/SKalt/card.git",
  "author": "Steven Kalt <kalt.steven@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "parcel build ./src/index.html --detailed-report",
    "start": "parcel ./src/index.html",
    "test": "cat ./dist/index.html | grep '\"snow_1'; echo $?",
    "deploy": "yarn run build && cp now.json ./dist && now ./dist"
  },
  "devDependencies": {
    "global": "^4.3.2",
    "parcel": "^1.11.0",
    "sass": "^1.15.2",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "lz-string": "^1.4.4"
  }
}
